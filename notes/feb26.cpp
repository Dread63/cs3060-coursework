#include <iostream>
#include <string>

struct test{

    int data;
};

int main() {

    using namespace std;

    char testChar = 'a';

    char upperTestChar = toupper(testChar);

    cout << upperTestChar << endl;

    struct data = new test;
    struct data2 = new test;

    bool operator==(const MyStruct& other) const {

        return (x == other.x && y == other.y)
    }

    return 0;
}
